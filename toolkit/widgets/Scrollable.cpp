#include <terminal/toolkit/Scrollable.hpp>

namespace terminal
{
    namespace toolkit
    {
        Scrollable::Scrollable(Composite *parent) :
            Control(parent)
        {
        }

        Scrollable::~Scrollable()
        {

        }
    }
}

