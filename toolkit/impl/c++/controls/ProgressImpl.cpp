#include <impl/c++/controls/ProgressImpl.h>

namespace terminal {
  namespace toolkit {
    namespace impl {
      ProgressImpl::ProgressImpl(terminal::toolkit::container_ptr container) :
        ControlImpl(container)
      {
      }

      ProgressImpl::~ProgressImpl() {
      }

      void ProgressImpl::redraw() {
      }

      void ProgressImpl::update() {
      }
    }
  }
}
