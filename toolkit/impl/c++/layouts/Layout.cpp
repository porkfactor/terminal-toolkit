#include <api/c++/Layout.hpp>

namespace terminal {
  namespace toolkit {
    Layout::Layout() {
    }

    Layout::~Layout() {
    }

    bool Layout::flushCache(control_ptr) {
      return(true);
    }
  }
}

